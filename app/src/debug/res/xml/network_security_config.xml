<?xml version="1.0" encoding="utf-8"?>
<!--
    Debug-only network policy.

    Cleartext is allowed exclusively for the loopback addresses used to reach a
    developer-machine Ollama instance:
      10.0.2.2  — emulator alias for the host loopback
      localhost — reached from a physical device via `adb reverse tcp:11434 tcp:11434`

    Everything else, including all model downloads, remains HTTPS-only even in
    debug. Model artifacts are integrity-checked by SHA-256 regardless (ADR-003),
    but transport security is not something to relax for convenience.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>

    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
